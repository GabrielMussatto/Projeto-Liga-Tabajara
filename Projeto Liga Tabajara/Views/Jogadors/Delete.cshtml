@model Projeto_Liga_Tabajara.Models.Jogador

@{
    ViewBag.Title = "Excluir Jogador";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    .delete-container {
        max-width: 600px;
        margin: 2rem auto;
        padding: 1.5rem;
        background-color: #f8f9fa;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

        .delete-container h2 {
            text-align: center;
            margin-bottom: 1rem;
            color: #c82333;
        }

        .delete-container p {
            text-align: center;
            font-size: 1.1rem;
            margin-bottom: 1.5rem;
            color: #333;
        }

        .delete-container dl dt {
            font-weight: 600;
            margin-top: 1rem;
            color: #555;
        }

        .delete-container dl dd {
            margin: 0 0 .75rem 0;
            padding-left: .5rem;
            color: #333;
        }

    .action-buttons {
        text-align: center;
        margin-top: 1.5rem;
    }

        .action-buttons .btn {
            margin: 0 .5rem;
            padding: .5rem 1.2rem;
            font-size: .9rem;
        }
</style>

<div class="delete-container">
    <h2>Excluir Jogador</h2>
    <p>Tem certeza de que deseja excluir este jogador?</p>

    <dl>
        <dt>@Html.DisplayNameFor(m => m.Time.Nome)</dt>
        <dd>@Html.DisplayFor(m => m.Time.Nome)</dd>

        <dt>@Html.DisplayNameFor(m => m.Nome)</dt>
        <dd>@Html.DisplayFor(m => m.Nome)</dd>

        <dt>@Html.DisplayNameFor(m => m.DataNascimento)</dt>
        <dd>@Html.DisplayFor(m => m.DataNascimento)</dd>

        <dt>@Html.DisplayNameFor(m => m.Nacionalidade)</dt>
        <dd>@Html.DisplayFor(m => m.Nacionalidade)</dd>

        <dt>@Html.DisplayNameFor(m => m.Posicao)</dt>
        <dd>@Html.DisplayFor(m => m.Posicao)</dd>

        <dt>@Html.DisplayNameFor(m => m.NumeroCamisa)</dt>
        <dd>@Html.DisplayFor(m => m.NumeroCamisa)</dd>

        <dt>@Html.DisplayNameFor(m => m.Altura)</dt>
        <dd>@Html.DisplayFor(m => m.Altura)</dd>

        <dt>@Html.DisplayNameFor(m => m.Peso)</dt>
        <dd>@Html.DisplayFor(m => m.Peso)</dd>

        <dt>@Html.DisplayNameFor(m => m.PePreferido)</dt>
        <dd>@Html.DisplayFor(m => m.PePreferido)</dd>
    </dl>

    <div class="action-buttons">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <input type="submit" value="Excluir" class="btn btn-danger" />
            @Html.ActionLink("Cancelar", "Index", null, new { @class = "btn btn-secondary" })
        }
    </div>
</div>